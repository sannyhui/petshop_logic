{{#each history}}
<div class="card card-body mb-2 text-center">
    <a href="/ua_invoice/{{invoice}}">{{invoice}}</a>
</div>
{{else}}
<p>No data listed</p>
{{/each}}

{{!-- 
{{#each ideas}} <!--// Loop ideas -->
<div class="card card-body mb-2">
    <h4>{{title}}</h4>
    <p>{{details}}</p>
    <a href="/ideas/edit/{{_id}}" class="btn btn-success btn-block">Edit</a>
    <form action="/ideas/{{_id}}?_method=DELETE" method="post">
    <input type="hidden" name="_method" value="DELETE"> <!-- Create a name called _method with value DELETE -->
    <input type="submit" class="btn btn-danger btn-block" value="Delete" />
    </form>
</div>
{{else}}
<p>No data listed</p>
{{/each}} --}}